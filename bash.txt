1.Bash assignments cannot have spaces around =
Wrong
script = "$(realpath -- "$BASHSOURCE[0]")"
script="$(realpath -- "$BASHSOURCE[0]")"
for file name, better to do input="f1.txt" , works best for input="my file.txt"

2. indentation doesnt matter in Bash

3. 0 means true in bash! 

4. && does the same thing as if statements

statement 1 && statement 2: only statements one success then run statement 2
|| does the oppsite, 1 || 2, 2 runs when 1 fails

nyk750@soar-9:~/BashBoom$ cd fol && echo "cant really cd"
bash: cd: fol: No such file or directory
nyk750@soar-9:~/BashBoom$ cd fol || echo "cant really cd"
bash: cd: fol: No such file or directory
cant really cd

5. -- can prevent filename starts with - being interpretered as option,
and should use "" around each $! but in echo it's optional!

6. single quote in bash means treates everything as they were
echo "number $i"
if single quotes, it will print number $i!

7.it has to be like this date +%Y-%m-%d_%H-%M-%S
after: no sapce, and put a space after date

8. if : [[ is stronger!]], but only for: [[ ... ]] is used to test conditions such as string comparison 
or file existence. It does not run id as a command.if you are gonan run a command inside, dont use any [] or [[]]
if command; then
    ...
fi

Notice here is a command! if you dont use [[]], u can use test


9. du -sh : s means summerize, -h means huamn readable; du -sh * | sort -h is good enough in most cases, 
but you can also filter with | tail -n 5!

10. a sym link is a pointer that points to a file or dir, ln -s Target link_name; remove sym link doesnt affect the file,  
but remove the file will make the sym link invalid
for a file:
for example i have a file at /home/1.txt after ln -s home/1.txt 1.txt(cant have this file/link exists beforehand), 
now cat/nano/modify affects both file.
use ls -l to check: or use readlink symlink!
lrwxrwxrwx 1 nyk750 nyk750    5 Jul 20 16:16 fake.sh -> rm.sh
remove sym link: just rm fake.sh or sth like this

for a dir:
ln -s dir/ data
then u can directly cd data, ls data!!!
when remove, just remove the link: rm data

11. tr means translation, it reads from standard input, replace or remove chars
syntax: tr set1 set2
echo "hello" | tr 'a-z' 'A-Z' --> capitalize! === tr '[:lower:]' '[:upper:]'
echo "banana" | tr a o ---> reaplce a with o!
OR 
nyk750@soar-9:~/BashBoom$ tr 'a-z' 'A-Z' < file.txt 
WHAT
ORnyk750@soar-9:~/BashBoom$ tr 'a-z' 'A-Z' <<< "what u talking about"
WHAT U TALKING ABOUT

SO IN BASH, THERE IS 3 types of input:
command < file(or /dev/null or /dev/urandom) # input from a file/path
command <<< "text"    # input from a string
command1 | command2   # input from another command

12. paste file1 file2 > mergedfile :
combines file line by line, side by side!

13. grep syntax:
grep "pattern" file

command | grep pattern: ps aux(all proc) | grep python 

common options: -i ignore cases/ -n show line num / grep -r "depthmean" src/ recursive
-v: prints the lines that does not match the pattern!
-c: count match lines / -w: match a complete word
-m1: means first match
grep "ERROR" file1 file2 : do multi files, output has it!
Regular expression:
grep "^ERROR" application.log : lines start with ERROR
grep "\.tif$" filename.txt 
notice here \. means .tif!
and in bash, '^$' means empty line!!!

Advanced:
grep -E "ERROR|WARNING" application.log : match either one
grep -E "[0-9]+" file.txt

14. In bash, you can always combine flags! like -rin for grep!

15. file descriptor: 0 std in /1 std out /2 std err
command > file          # stdout only
command 2> file         # stderr only
command &> file         # stdout and stderr together
command &>> file        # append stdout and stderr together

and dont mix it with 2>&1!! put error into std output

16. in bash, bith [] and [[]] needs space around them, exclude;

17.$? means the execute status of last command, 0 means success!
but for arithmetic: if (( 0 )); then echo "True" else echo "False" fi -> False

18. Common User command
id username 
whoami
#show groups user belong to
groups username
groups bob

#List currently logged in users
who / w

sudo useradd username
sudo useradd -m bob # create a user with home dir

sudo passwd username #change user's pwd
# lock/unlock a user
sudo passwd -l/-u username
or sudo usermod -L/-U username

sudo usermod -aG group username
sudo suerdel username
#display all users
getent passwd

19. find the current loggin users:
who | awk '{print $1}' | sort | uniq
and sort -u === sort | uniq

20. cut -d: -f2- 
prints through f2 to teh end, -d means delimiter
=== --delimiter=':'

21. in bash, be careful when name / field has a space in it, use " "!!!
grep "model name" /proc/cpuinfo
grep model name /proc/cpuinfo --- Wrong!!!
and the quotes itself is optional if the name/field has no space

22. Inside Bash arithmetic expansion, variable names do not need $
used_kn=$((total_kb - avail_kb))
for +,-,*,/, use (()) for all!

23. -F sets the separator
loads=$(uptime | awk -F'load average: ' '{print $2}')
awk -F'load average: ' '{print $2}'

-F'load average: ' sets load average: as the separator.
$1 is everything before that separator.
$2 is everything after it.

24. another for of loop:
for i in {1..3}; do

done

25. awk syntax: THE pattern here mostly is which column which row!
1. input | awk 'pattern { action }'
2. awk 'pattern { action }' file

26. pgrep searches running processes and prints their process IDs:
pgrep python, add -a to print extra info(display full command line), or -af
-af -> search + display full command line, output: 18342 python train.py --epochs 100
pgrep -u nyk750 owned by nyk750
pgrep -x python exact name match
pgrep -n python newest matching
pgrep -o python oldest matching

-> kill '$(pgrep -n python)'

27.xxx | tr -dc '0-9' : tranalstions the given input and delete everything taht's not a digits

28. in echo: you can have $variable === ${variable}, but {} is just more clear

29. array in bash: use space to separate not comma
hosts=('a' 'b' 'c')
echo "${hosts[0]}"
echo "${hosts[1]}"
echo "${hosts[2]}"

all echo "${hosts[@]}"

30. fold command!
fold wraps long lines into shorter lines - does not cut chars but move them to next line

echo "abcdefgihsnsbeybszi" | fold -w 5
-> abcde ...

or u can wrap a entire file - each line-wise
fold -s -w 20 file.txt 
fold file.txt (default 80 chars) 

31. In nowadays bash:
inside if and while conditions or between commands!
and: &&
or: ||
not: !

Notice for if and while command, u have to use [[ if you use those inside ]]
or just [] those []!

32. -z "$username" checks if the username is empty string
-n "username" checcks if the username is NOTEMPTY! 
if [ -n "$useranme" ] ... : then it means username is not empty!
